import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    containerInputs: {
        marginVertical: 20,
        alignItems: "center",
        justifyContent: "space-between",
    },
});